import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ami.discipline',
  appName: 'Discipline Tracker ',
  webDir: 'public'
};

export default config;
